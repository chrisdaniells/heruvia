const colours = {
    primary: '#790101',
    secondary: '#428645',
    line: '#d2d2d2',
    background: '#f1f1f1',
    text: {
        default: '#000000'
    },
};

const spacing = {
    default: 20,
    thin: 10,
};

const container = {
    maxWidth: 1200,
    minWidth: 800,
    width: '100%',
    margin: '0 auto',
    padding: '0 ' + spacing.default + 'px',
}

export default {
    colours,
    spacing,
    container
}